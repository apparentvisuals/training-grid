import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apparentvisuals.traininggrid',
  appName: 'Training Grid',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
